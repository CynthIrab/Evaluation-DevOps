name: Deploy Runner

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on:
      - self-hosted
      - linux
      - x64

    steps:
      - uses: actions/checkout@v4

      - name: Deploy site
        run: |
          sudo cp index.html /var/www/html/index.html
